# MyTag 使用指南

`mytag` 是 MD Notebook 的 Git tag 管理工具，用于自动创建和推送版本标签，触发 GitHub Actions 自动化发布。

## 🎯 功能特性

- ✅ 自动读取 `version.json` 中的当前版本号
- ✅ 将内部版本格式转换为 Git tag 格式
- ✅ 检查未提交的更改
- ✅ 检查 tag 是否已存在
- ✅ 自动创建带注释的 Git tag
- ✅ 自动推送 tag 到远程仓库
- ✅ 触发 GitHub Actions 自动构建

## 📋 版本号格式

### 内部版本格式
```
Ver:1.0.20260206.007
```

格式说明：
- `Ver:` - 版本前缀
- `1.0` - 主版本号.次版本号
- `20260206` - 日期（YYYYMMDD）
- `007` - 当日构建号（3位数字）

### Git Tag 格式
```
v1.0.20260206.007
```

格式说明：
- `v` - Git tag 前缀
- 其余部分与内部版本号相同

## 🚀 使用方法

### 完整发布流程

```bash
# 1. 更新版本号
bun run upgrade "添加新功能"

# 2. 提交代码
git add .
git commit -m "feat: 添加新功能"
git push

# 3. 创建并推送 tag（触发自动发布）
bun run mytag
```

### 单独使用 mytag

```bash
# 直接运行
bun run mytag

# 或使用完整路径
bun scripts/mytag.ts
```

## 📝 工作流程

### 1. 读取版本信息

脚本会自动读取 `version.json` 文件：

```json
{
  "version": "Ver:1.0.20260206.007",
  "changelog": [
    {
      "version": "Ver:1.0.20260206.007",
      "date": "2026-02-06",
      "changes": "添加新功能"
    }
  ]
}
```

### 2. 显示版本信息

```
🏷️  MD Notebook Git Tag 管理工具

📦 当前版本: Ver:1.0.20260206.007
🏷️  Git Tag: v1.0.20260206.007
📝 最新变更: 添加新功能
📅 变更日期: 2026-02-06
```

### 3. 检查未提交更改

如果有未提交的更改，会提示：

```
⚠️  检测到未提交的更改
   建议先提交所有更改后再创建 tag

   运行以下命令提交更改：
   git add .
   git commit -m "your message"
   git push

是否继续创建 tag？(y/N):
```

### 4. 创建并推送 tag

```
🔨 创建 tag: v1.0.20260206.007...
✅ Tag 创建成功

📤 推送 tag 到远程仓库...
✅ Tag 推送成功

🎉 完成！GitHub Actions 将自动开始构建
```

### 5. 后续步骤提示

```
📋 后续步骤：
   1. 访问 GitHub 仓库的 Actions 标签查看构建进度
   2. 构建完成后，在 Releases 页面查看发布草稿
   3. 编辑发布说明并正式发布

🔗 GitHub Actions: https://github.com/<your-repo>/actions
🔗 Releases: https://github.com/<your-repo>/releases
```

## ⚠️ 注意事项

### Tag 已存在

如果 tag 已存在，会显示错误：

```
❌ Tag v1.0.20260206.007 已存在
   如需重新创建，请先删除旧 tag：
   git tag -d v1.0.20260206.007
   git push origin :refs/tags/v1.0.20260206.007
```

### 推送失败

如果推送失败，可能是：

1. **未配置远程仓库**
   ```bash
   git remote add origin <repository-url>
   ```

2. **没有推送权限**
   - 检查 GitHub 账号权限
   - 确认 SSH 密钥或 HTTPS 凭据配置正确

3. **网络问题**
   - 检查网络连接
   - 尝试使用 VPN

## 🔄 与其他命令的配合

### 完整的版本发布流程

```bash
# 步骤 1: 开发新功能
# ... 编写代码 ...

# 步骤 2: 运行测试
bun test

# 步骤 3: 更新版本号
bun run upgrade "添加 XXX 功能"

# 步骤 4: 提交代码（使用 mygit）
bun run mygit

# 步骤 5: 创建 tag 并触发发布
bun run mytag
```

### 快速发布（已提交代码）

```bash
# 如果代码已提交，只需：
bun run upgrade "修复 XXX 问题"
bun run mytag
```

## 🛠️ 故障排除

### 问题 1: 找不到 version.json

**错误信息**：
```
❌ 无法读取 version.json 文件
```

**解决方案**：
- 确保在项目根目录运行命令
- 检查 `version.json` 文件是否存在

### 问题 2: Git 命令失败

**错误信息**：
```
❌ 创建 tag 失败
```

**解决方案**：
- 确保已安装 Git
- 检查是否在 Git 仓库中
- 运行 `git status` 检查仓库状态

### 问题 3: 权限问题

**错误信息**：
```
❌ 推送 tag 失败
Permission denied
```

**解决方案**：
- 配置 SSH 密钥：`ssh-keygen -t ed25519 -C "your_email@example.com"`
- 添加到 GitHub：Settings > SSH and GPG keys
- 或使用 HTTPS 并配置凭据

## 📚 相关文档

- [版本管理指南](VERSION_MANAGEMENT.md)
- [MyGit 使用指南](MYGIT_USAGE.md)
- [GitHub Actions 指南](GITHUB_ACTIONS_GUIDE.md)

## 💡 最佳实践

1. **先测试后发布**
   ```bash
   bun test  # 确保所有测试通过
   ```

2. **有意义的变更说明**
   ```bash
   bun run upgrade "修复文件保存时的编码问题"  # ✅ 好
   bun run upgrade "修复 bug"                    # ❌ 不好
   ```

3. **定期发布**
   - 积累一定功能后再发布
   - 避免频繁发布小更新

4. **检查构建状态**
   - 推送 tag 后立即查看 GitHub Actions
   - 确保所有平台构建成功

## 🎯 示例场景

### 场景 1: 首次发布

```bash
# 1. 确保代码已提交
git status

# 2. 更新版本号
bun run upgrade "首次正式发布"

# 3. 提交版本文件
git add version.json
git commit -m "chore: 准备首次发布"
git push

# 4. 创建 tag
bun run mytag
```

### 场景 2: 紧急修复

```bash
# 1. 修复 bug
# ... 修改代码 ...

# 2. 测试
bun test

# 3. 快速发布
bun run upgrade "紧急修复：解决崩溃问题"
bun run mygit  # 自动提交
bun run mytag  # 自动发布
```

### 场景 3: 功能更新

```bash
# 1. 完成功能开发
# ... 开发新功能 ...

# 2. 更新文档
# ... 更新 README 等 ...

# 3. 完整流程
bun test
bun run upgrade "新增：Markdown 表格编辑功能"
bun run mygit
bun run mytag
```
