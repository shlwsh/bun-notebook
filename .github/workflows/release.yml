name: MD Notebook 跨平台发布

on:
  push:
    tags:
      - 'v*'  # 当推送 v 开头的 tag 时触发，如 v1.0.0
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write  # 允许创建 GitHub Releases

jobs:
  build-and-release:
    strategy:
      fail-fast: false  # 一个平台失败不影响其他平台
      matrix:
        include:
          # macOS Intel (x64)
          - platform: 'macos-latest'
            args: ''
            
          # macOS Apple Silicon (ARM64)
          - platform: 'macos-latest'
            args: '--target aarch64-apple-darwin'
            
          # Linux x64
          - platform: 'ubuntu-22.04'
            args: ''
            
          # Windows x64
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 安装 Linux 依赖
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.0-dev \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            build-essential \
            curl \
            wget \
            libssl-dev \
            libgtk-3-dev

      - name: 安装 Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: 设置 Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.args && contains(matrix.args, '--target') && split(matrix.args, ' ')[1] || '' }}

      - name: 缓存 Rust 依赖
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: 安装前端依赖
        run: bun install

      - name: 构建前端
        run: bun run build

      - name: 构建 Tauri 应用
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'MD Notebook ${{ github.ref_name }}'
          releaseBody: |
            ## MD Notebook ${{ github.ref_name }}
            
            ### 📦 下载安装包
            
            请根据您的操作系统选择对应的安装包：
            
            - **macOS (Intel)**: `.dmg` 文件
            - **macOS (Apple Silicon)**: `_aarch64.dmg` 文件
            - **Windows**: `.msi` 或 `.exe` 文件
            - **Linux**: `.deb` 或 `.AppImage` 文件
            
            ### ✨ 更新内容
            
            查看完整更新日志请访问 [CHANGELOG](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md)
            
            ### 📧 联系方式
            
            如有问题或建议，请联系：shlwsh@126.com
          releaseDraft: true  # 创建草稿，手动审核后发布
          prerelease: false
          args: ${{ matrix.args }}

      - name: 上传构建日志（失败时）
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ matrix.platform }}-${{ matrix.args }}
          path: |
            target/release/*.log
            src-tauri/target/release/*.log
          retention-days: 7
